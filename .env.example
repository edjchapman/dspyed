# Copy to .env (gitignored). Loaded by the dspyed CLI at startup.

# Required for live eval/compile runs (LiteLLM reads it from the environment).
ANTHROPIC_API_KEY=

# Optional overrides — defaults live in src/dspyed/config.py (env prefix DSPYED_).
# DSPYED_SMALL_MODEL=anthropic/claude-haiku-4-5-20251001
# DSPYED_LARGE_MODEL=anthropic/claude-sonnet-5
# DSPYED_BUDGET_CAP_USD=150
